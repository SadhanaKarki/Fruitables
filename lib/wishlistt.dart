import 'package:flutter/material.dart';

class Wishlistt extends StatelessWidget {
  const Wishlistt({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}